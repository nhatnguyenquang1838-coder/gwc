# SCRUM-149

Run the real bounded host-mode and offline-consumer acceptance path. Fixture
examples are inputs and regression oracles; they are not by themselves proof
of real provider calls. Preserve exact package/source/ref provenance and typed
failure behavior at every adapter boundary.
