name: Notion MCP
version: 0.0.1
schema: v1

# Endpoint and auth policy are managed centrally in .continue/user-setup.yaml
mcpServers:
  - name: Notion
    type: stdio
    command: npx
    args:
      - "-y"
      - "mcp-remote"
      - "https://mcp.notion.com/mcp"