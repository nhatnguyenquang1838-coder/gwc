{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gwc.local/schemas/runtime/decision-rule-registry.schema.json",
  "title": "Canonical Typed Decision Rule Registry",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "artifact_type", "registry_id", "revision", "rules", "provenance"],
  "properties": {
    "schema_version": {"type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$"},
    "artifact_type": {"const": "decision-rule-registry"},
    "registry_id": {"type": "string", "minLength": 1},
    "revision": {"type": "string", "minLength": 1},
    "rules": {"type": "array", "minItems": 1, "items": {"$ref": "decision-rule.schema.json"}},
    "provenance": {"type": "string", "minLength": 1}
  }
}
