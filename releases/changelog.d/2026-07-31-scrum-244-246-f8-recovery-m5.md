# SCRUM-244/245/246 — failure_recovery M5 batch

## Added

- `failure_recovery.approval-expiry-recovery` M5 decision utility, JSON Schema, and expiry/replay/checkpoint fixtures.
- `failure_recovery.duplicate-agent-fencing` M5 decision utility, JSON Schema, and lease/fencing/race/stale-worker fixtures.
- `failure_recovery.version-drift-rollback-routing` M5 decision utility, JSON Schema, and compatibility/replay/evidence preservation fixtures.

## Validation

- `python -m unittest tests/test_failure_recovery_m5_batch.py` — PASS, 16 tests.
- `python -m compileall -q tools` — PASS.
- PR GitHub Actions remain required for full repository validation.

## Boundaries

No merge, deploy, release, production configuration, production data, secrets, credential rotation, migration, force-push, branch deletion, PR base change, package version change, or unrelated cleanup.
