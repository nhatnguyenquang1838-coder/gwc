schema_version: "1.0"
artifact_type: g1-intake-brief
generated_at: "2026-07-22T18:50:12Z"
trace:
  project_id: gwc
  repository: nhatnguyenquang1838-coder/gwc
  task_id: task_e7c25957-860f-412f-981d-fd56afa28671
  base_sha: 34c080a3fa82a19182b04a8be2a784a8e2ab5acd
  g0_snapshot: ../../g0/context-snapshot.yaml
problem:
  statement: The current GWC graph was assembled without the official semantic file-analysis stage, so relationships such as gate-to-runbook traversal are incomplete or misleading.
  why_now: The user explicitly requested a full official Understand Anything rebuild after observing disconnected graph relationships.
desired_outcome: Rebuild GWC's .ua knowledge graph through the official full pipeline and verify that content-grounded semantic relationships and dashboard traversal are represented correctly.
stakeholders:
  requester: User
  affected: [GWC maintainers, Understand Anything dashboard users]
scope:
  in_scope:
    - Read and apply the official Understand Anything understand and dashboard skills
    - Full scan, batch, deterministic structure extraction, semantic file analysis, merge/review, architecture, tour, validation, and fingerprint/meta stages
    - Regenerate .ua/knowledge-graph.json and its official intermediate/meta outputs
    - Validate gate/runbook relationship evidence and dashboard graph loading/traversal
  non_goals:
    - Editing GWC source governance semantics to manufacture graph edges
    - Hand-editing generated graph output after the official pipeline
    - Fixing unrelated pre-existing working-tree changes
    - Merge, deployment, production configuration/data, credential, or secret operations
constraints:
  - Preserve unrelated dirty files and inspect generated diff for scope drift
  - Use Claude/official Understand Anything subagent execution if available; report semantic capability gaps honestly
  - Do not write directly to protected main; execute on a dedicated guarded branch/worktree
assumptions:
  - .ua/.understandignore is the reviewed ignore configuration for this run
  - The current graph's base commit is 34c080a3fa82a19182b04a8be2a784a8e2ab5acd
  - The local Understand Anything clone is the available upstream skill source
risks:
  - id: RISK-1
    description: LLM semantic analysis may add unsupported relationships if prompts or evidence handling drift from the official agent contract.
    impact: medium
    mitigation: Use official file-analyzer/merge/reviewer prompts, require content-grounded edges, and run deterministic graph validation.
  - id: RISK-2
    description: Existing dirty generated artifacts and unrelated files could be overwritten or mixed into the rebuild.
    impact: medium
    mitigation: Isolate execution in a dedicated worktree and compare only scoped .ua outputs against the protected base.
acceptance_criteria:
  - id: AC-1
    statement: The official full pipeline completes without an unreported phase failure and produces schema-valid knowledge-graph.json, meta.json, and fingerprints.json.
    verifiable: true
  - id: AC-2
    statement: Every emitted semantic edge is grounded in analyzed file content and no orphan or dangling graph references remain.
    verifiable: true
  - id: AC-3
    statement: A content-grounded path query for GATE_G0_G1_OPERATIONAL_RUNBOOK_v1.0.md and GATE_LIFECYCLE_CONTRACT_v1.0.md returns the expected relationship when one exists, and the dashboard loads the rebuilt graph over HTTP.
    verifiable: true
delivery_lifecycle:
  authorized_actions:
    - Run official Understand Anything analysis in the dedicated guarded worktree
    - Write only task-scoped generated .ua outputs and validation evidence
    - Run local validation and inspect the complete scoped diff
  downstream_non_goals: [G4_MERGE, G5_DEPLOY, G6_PRODUCTION]
  g3_metadata_completion:
    ready_for_review_after_g3_pass: false
    required_evidence: [scoped diff review, graph validation, dashboard HTTP smoke test]
unresolved_questions: []
status: READY
