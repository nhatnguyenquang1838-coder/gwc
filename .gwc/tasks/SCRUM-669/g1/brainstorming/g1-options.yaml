# run_id: g1-scrum-669-20260908-1659
schema_version: '1.0'
artifact_type: g1-options
generated_at: '2026-09-08T16:59:20Z'
trace:
  project_id: gwc
  repository: nhatnguyenquang1838-coder/gwc
  task_id: SCRUM-669
  base_sha: 0e752b04c9f40a04fe402a4f25fcb12c8b9b4d72
  g0_snapshot: ../../g0/context-snapshot.yaml
options:
- id: OPT-1
  title: Refresh G1/G2 semantic contract, then execute the bounded seven-probe F5/F6 wave
  description: >-
    Reconcile the fresh current-subject G0/G1 packet, bind the seven RED-to-GREEN
    probes and sibling controls as acceptance criteria, recalculate the canonical
    four-path scope hash, and obtain a new executable G2 token before any edit.
  benefits:
  - Prevents the superseded token from authorizing the expanded behavioral contract.
  - Covers runtime/schema parity, canonical references, padded self-loop rejection, and control/reference controls.
  - Preserves the unchanged four-path write scope and receipt-schema read-only boundary.
  tradeoffs:
  - Requires a second governance reconciliation and fresh G2 token.
  risks:
  - Implementation remains blocked until the refreshed packet is remotely verified.
  constraint_fit: FIT
- id: OPT-2
  title: Continue under the superseded G2 token
  description: >-
    Reuse CP-20260908-SCRUM669-G2-F5F6-COMPAT-R1 despite the semantic contract
    expanding from the prior approved behavior.
  benefits:
  - Avoids a governance refresh.
  tradeoffs:
  - The token is explicitly superseded for implementation purposes.
  risks:
  - Would execute an expanded behavior contract under stale authority.
  constraint_fit: NOT_FIT
- id: OPT-3
  title: Expand the writable scope to include the receipt schema
  description: >-
    Add child-run-materialization-receipt.schema.json to the write scope to fix
    every observed reference-schema concern in one wave.
  benefits:
  - Could address receipt-schema padding directly.
  tradeoffs:
  - Contradicts the approved read/validation-only boundary.
  risks:
  - Out-of-scope authority expansion and stale-scope failure.
  constraint_fit: NOT_FIT
recommended_option_id: OPT-1
recommendation_rationale: >-
  OPT-1 is the only option that honors GPT's supersession decision, refreshes the
  semantic acceptance contract before implementation, keeps the exact four-path
  write boundary, and preserves the receipt schema as read/validation-only.
decision_required: true
status: READY
