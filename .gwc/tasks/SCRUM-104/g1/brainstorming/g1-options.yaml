schema_version: '1.0'
artifact_type: g1-options
generated_at: '2026-07-26T07:23:37Z'
trace:
  project_id: gwc
  repository: nhatnguyenquang1838-coder/gwc
  task_id: SCRUM-104
  base_sha: 43daebcffbd71cf0339c4de8c82d3c91db95be1d
  g0_snapshot: ../../g0/context-snapshot.yaml
options:
  - id: OPT-1
    title: GWC-local contract-first registries and v3 adapter
    description: Add source-backed node, scenario and profile registries, typed schemas, cross-registry validators and the GWC-local data-driven v3 adapter described by the protected-base design.
    benefits:
      - Meets the complete SCRUM-104 objective within one repository.
      - Preserves the 81-slot invariant and explicit maturity/provenance.
      - Enables negative tests for runtime versus visual-only edges.
    tradeoffs:
      - Adds a new viewer adapter surface because no current Cytoscape implementation is present.
      - Requires package/export and integrity readback.
    risks:
      - A new viewer surface could drift from the eventual owning UI if a separate consumer is later identified.
    constraint_fit: FIT
  - id: OPT-2
    title: Registries and validators only
    description: Materialize the canonical registries and validators while deferring the v3 data binding.
    benefits:
      - Smaller initial implementation surface.
    tradeoffs:
      - Does not satisfy the full v3 acceptance criteria.
    risks:
      - Leaves hard-coded visualization behavior unresolved.
    constraint_fit: PARTIAL
  - id: OPT-3
    title: Multi-repository v3 consumer change
    description: Locate and change an external Cytoscape v3 consumer repository together with GWC contracts.
    benefits:
      - Could bind the actual owning UI directly.
    tradeoffs:
      - Requires a second repository, task, base SHA and approval envelope.
    risks:
      - Cross-repository scope and authority are not currently verified.
    constraint_fit: NOT_FIT
recommended_option_id: OPT-1
recommendation_rationale: The protected-base design specifies a GWC-local viewer adapter path, while the current repository search found no external Cytoscape implementation. This option satisfies SCRUM-104 without silently expanding to another repository.
decision_required: true
status: READY
