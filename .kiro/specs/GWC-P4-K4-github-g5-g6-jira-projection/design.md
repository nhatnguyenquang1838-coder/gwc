# Design Document

Add three machine-readable evidence schemas, examples, a validator and unit tests. The validator fails closed on stale main, PR-head drift, absent/failing CI, merge-SHA mismatch and Jira projection authority leakage.
